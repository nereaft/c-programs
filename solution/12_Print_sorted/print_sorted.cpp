#include <string.h>
#include <stdio.h>
#include <stdlib.h>


int main (int argc, char *argv[]){

    const char * list[] = {"yield", "yelp", "yellow" };

    int valor_01 = strcmp(list[0],list [1]);
    int valor_02 =  strcmp(list[0],list [1]);

    if(valor_01 == 0 & 
	printf(" La palabra %s es igual a la palabra %s ", list[0],list[1]);
    }else{
	if(valor > 0)
	    printf("La palabra %s es mayor que %s ", list[0],list[1]);
	else
	    printf("La palabra %s es menor que %s ", list[0],list[1]);
    }

    printf("\n");





    return EXIT_SUCCESS;
}

