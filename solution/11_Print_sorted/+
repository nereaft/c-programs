#include <stdio.h>
#include <stdlib.h>
#include <string.h>



int main (int argc, char *argv[]){

    const char * list[] = { "yield", "yelp"};

    int valor = strcmp(list[0],list [1]);

    if(valor == 0 ){
	printf(" La palabra %s es igual a la palabra %s ", list[0],list[1]);
    }else{
	if(valor > 0)
	    printf("La palabra %s es mayor que %s ", list[0],list[1]);
	else 
	    printf("La palabra %s es menor que %s ", list[0],list[1]);
    }

    printf("\n");





    return EXIT_SUCCESS;
}
